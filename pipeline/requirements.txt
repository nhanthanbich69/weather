# ============================================================
# Weather Data Pipeline - Python Dependencies
# ============================================================
# Install with: pip install -r requirements.txt
# 
# FULL PIPELINE: Crawl → Supabase → DuckDB → Power BI
# ============================================================

# Core Data Processing
pandas>=2.0.0
numpy>=1.24.0

# Data Collection & APIs
requests>=2.31.0
geopy>=2.3.0      # Geocoding for location resolution
urllib3>=2.0.0

# Cloud & Storage
supabase>=2.3.0   # Supabase client for cloud storage
psycopg2-binary>=2.9.0  # PostgreSQL adapter (OPTIMIZED: Direct DB connection for bulk insert)
python-dotenv>=1.0.0  # Environment variable management

# Data Warehouse & Analytics
duckdb>=0.9.0     # In-process SQL database for analytics
duckdb-engine>=0.12.0  # SQLAlchemy integration (optional)

# Logging
python-json-logger>=2.0.0
